import { DotNumberDirective } from './dot-number.directive';

describe('DotnumberDirective', () => {
  it('should create an instance', () => {
    const directive = new DotNumberDirective();
    expect(directive).toBeTruthy();
  });
});
